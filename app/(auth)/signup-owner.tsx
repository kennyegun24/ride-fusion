import { StyleSheet, Text, View } from "react-native";
import React from "react";

const signupOwner = () => {
  return (
    <View>
      <Text>signupOwner</Text>
    </View>
  );
};

export default signupOwner;

const styles = StyleSheet.create({});
