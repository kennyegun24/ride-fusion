import { StyleSheet, Text, View } from "react-native";
import React from "react";

const signupDriver = () => {
  return (
    <View>
      <Text>signupDriver</Text>
    </View>
  );
};

export default signupDriver;

const styles = StyleSheet.create({});
