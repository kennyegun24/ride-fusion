import { StyleSheet, Text, View } from "react-native";
import React from "react";

const NoReview = () => {
  return (
    <View style={{ height: 100 }}>
      <Text style={{ margin: "auto" }}>No Review found</Text>
    </View>
  );
};

export default NoReview;

const styles = StyleSheet.create({});
